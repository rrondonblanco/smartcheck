{
  "name": "smartcheck-server",
  "version": "0.1.0",
  "description": "SmartCheck MVP backend — Express + mock DB. Handoff target: equipo TI Reuse.",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
